import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';


const panel_why: Routes = [
  
];

@NgModule({
  imports: [RouterModule.forRoot(panel_why)],
  exports:[RouterModule]
})
export class PanelWhyRoutingModule { }
